# =============================================
# KISANSETU - SMART FARMER EXCHANGE
# Spring Boot + MongoDB Configuration
# =============================================

# Application
spring.application.name=kisansetu-backend
server.port=8081

# Embedded MongoDB Version (for development without local MongoDB install)
# de.flapdoodle.mongodb.embedded.version=7.0.4

# MongoDB Configuration - LOCAL (uses embedded if not specified)
# spring.data.mongodb.uri=mongodb://localhost:27017/kisansetu

# MongoDB Atlas - FREE CLOUD (uncomment and replace with your Atlas connection string)
spring.data.mongodb.uri=mongodb+srv://kisansetu:kisan123@kisansetu.dpwb6h4.mongodb.net/kisansetu?retryWrites=true&w=majority&appName=kisansetu

# DEMO MODE: Uncomment below to run WITHOUT database (in-memory only for testing)
# spring.data.mongodb.uri=mongodb://localhost:27017/kisansetu
# spring.datasource.url=jdbc:h2:mem:testdb

# Logging
logging.level.org.springframework.data.mongodb=DEBUG
logging.level.com.kisansetu=DEBUG

# CORS Configuration
cors.allowed-origins=*

# JSON pretty print
spring.jackson.default-property-inclusion=non_null
